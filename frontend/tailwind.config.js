/** @type {import('tailwindcss').Config} */
module.exports = {
    content: [
        "./src/**/*.{js,jsx,ts,tsx}", // Look at all your React files
    ],
    theme: {
        extend: {}, // You can add custom colors, fonts, spacing later
    },
    plugins: [], // No plugins needed for now
}
/** @type {import('tailwindcss').Config} */
module.exports = {
    content: ["./src/**/*.{js,jsx,ts,tsx}"],
    theme: {
        extend: {},
    },
    plugins: [],
}